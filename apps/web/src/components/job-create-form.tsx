import { createJob } from "@/features/workspace/actions";
import { SubmitButton } from "@/components/submit-button";

export function JobCreateForm() {
  return <form className="job-create-form" action={createJob}><section className="job-form-section"><header><h3>Role details</h3><p>The essentials you need to recognize and compare this role.</p></header><div className="job-form-fields"><div className="field-grid"><div className="field"><label htmlFor="company">Company</label><input className="input" id="company" name="company" autoComplete="organization" required /></div><div className="field"><label htmlFor="title">Role title</label><input className="input" id="title" name="title" autoComplete="organization-title" required /></div></div><div className="field-grid"><div className="field"><label htmlFor="location">Location</label><input className="input" id="location" name="location" autoComplete="address-level2" /></div><div className="field"><label htmlFor="compensation">Compensation</label><input className="input" id="compensation" name="compensation" placeholder="$80k–$100k" /></div></div><div className="field"><label htmlFor="remotePolicy">Remote policy</label><input className="input" id="remotePolicy" name="remotePolicy" placeholder="Remote worldwide, hybrid, on-site…" /></div></div></section><section className="job-form-section"><header><h3>Source and context</h3><p>Keep the original listing and enough detail for a useful review.</p></header><div className="job-form-fields"><div className="field-grid"><div className="field"><label htmlFor="sourceUrl">Job URL</label><input className="input" id="sourceUrl" name="sourceUrl" type="url" inputMode="url" placeholder="https://…" /></div><div className="field"><label htmlFor="applicationUrl">Application URL</label><input className="input" id="applicationUrl" name="applicationUrl" type="url" inputMode="url" placeholder="https://…" /></div></div><div className="field"><label htmlFor="description">Job description</label><textarea className="textarea job-description-input" id="description" name="description" placeholder="Paste the role description, responsibilities, and requirements…" /></div></div></section><footer className="job-create-actions"><p>Saved jobs stay in your Inbox until you choose to track them.</p><SubmitButton pendingLabel="Saving job…">Save to inbox</SubmitButton></footer></form>;
}
