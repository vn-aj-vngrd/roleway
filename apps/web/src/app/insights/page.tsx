export default function InsightsPage() {
  return <div className="page narrow"><header className="page-header"><div className="page-header-copy"><h1>Insights</h1><p className="page-subtitle">Last 30 days · enough activity for directional conversion data.</p></div><button className="button secondary">Last 30 days</button></header><section className="insight-grid" aria-label="Pipeline summary"><div className="metric"><div className="metric-value">14</div><div className="metric-label">Applications submitted</div></div><div className="metric"><div className="metric-value">6</div><div className="metric-label">Replies received</div></div><div className="metric"><div className="metric-value">3</div><div className="metric-label">Interviews reached</div></div></section><section className="conversion-table"><div className="content-section-head"><div><h2>Pipeline conversion</h2><p className="page-subtitle">Closed and still-active applications are included.</p></div></div>{[{ label: "Application → Reply", value: 43 }, { label: "Reply → Interview", value: 50 }, { label: "Interview → Offer", value: 33 }].map((metric) => <div className="conversion-row" key={metric.label}><span>{metric.label}</span><strong className="mono">{metric.value}%</strong><div className="progress"><span style={{ width: `${metric.value}%` }} /></div></div>)}</section><section className="content-section" style={{ marginTop: 36 }}><h2>What the data supports</h2><p className="prose" style={{ marginTop: 8 }}>Applications with fit analyses above 85 have produced 5 of 6 replies. The sample is still small, so treat this as a useful sorting signal—not proof that fit score causes replies.</p></section></div>;
}
