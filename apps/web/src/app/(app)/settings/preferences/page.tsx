import { SettingsNav } from "@/components/settings-nav";
import { SubmitButton } from "@/components/submit-button";
import { requireUser } from "@/lib/supabase/server";
import { updatePreferences } from "../actions";

export default async function PreferencesPage({ searchParams }: { searchParams: Promise<{ saved?: string; error?: string }> }) {
  const [auth, query] = await Promise.all([requireUser(), searchParams]); if (!auth) return null;
  const { data: preferences, error } = await auth.supabase.from("career_preferences").select("target_titles, preferred_technologies, remote_preference, allowed_locations, minimum_compensation, currency, excluded_criteria").eq("user_id", auth.user.id).maybeSingle();
  return <div className="page settings-page"><header className="page-header"><div><h1>Settings</h1><p className="page-subtitle">Tune Roleway around the way you work.</p></div></header><div className="settings-layout"><SettingsNav active="Job preferences" /><main>{query.saved ? <div className="form-alert success" role="status">Job preferences saved.</div> : null}{query.error || error ? <div className="form-alert error" role="alert">{query.error || "Your preferences could not be loaded."}</div> : null}<form action={updatePreferences}><section className="form-section"><h2>Role and skills</h2><p>Use commas to separate values. These preferences help you evaluate fit consistently.</p><div className="field"><label htmlFor="targetTitles">Target roles</label><input className="input" id="targetTitles" name="targetTitles" defaultValue={preferences?.target_titles?.join(", ") ?? ""} placeholder="Product Engineer, Full-Stack Engineer" /></div><div className="field"><label htmlFor="technologies">Preferred technologies</label><input className="input" id="technologies" name="technologies" defaultValue={preferences?.preferred_technologies?.join(", ") ?? ""} placeholder="TypeScript, React, PostgreSQL" /></div></section><section className="form-section"><h2>Location and compensation</h2><p>Set practical boundaries before evaluating individual listings.</p><div className="field-grid"><div className="field"><label htmlFor="remotePreference">Remote preference</label><select className="input" id="remotePreference" name="remotePreference" defaultValue={preferences?.remote_preference ?? "flexible"}><option value="required">Remote required</option><option value="preferred">Remote preferred</option><option value="flexible">Flexible</option></select></div><div className="field"><label htmlFor="locations">Allowed locations</label><input className="input" id="locations" name="locations" defaultValue={preferences?.allowed_locations?.join(", ") ?? ""} placeholder="Remote, Manila, Singapore" /></div></div><div className="compensation-grid"><div className="field"><label htmlFor="minimumCompensation">Minimum compensation</label><input className="input" id="minimumCompensation" name="minimumCompensation" type="number" min="0" defaultValue={preferences?.minimum_compensation ?? ""} /></div><div className="field"><label htmlFor="currency">Currency</label><select className="input" id="currency" name="currency" defaultValue={preferences?.currency ?? "USD"}><option>USD</option><option>PHP</option><option>EUR</option><option>GBP</option><option>SGD</option><option>AUD</option></select></div></div></section><section className="form-section"><h2>Hard boundaries</h2><p>List deal-breakers you want to catch during review.</p><div className="field"><label htmlFor="excludedCriteria">Avoid</label><textarea className="textarea" id="excludedCriteria" name="excludedCriteria" rows={4} defaultValue={preferences?.excluded_criteria?.join(", ") ?? ""} placeholder="Night shifts, mandatory relocation, unpaid trial work" /></div><SubmitButton pendingLabel="Saving…">Save preferences</SubmitButton></section></form></main></div></div>;
}
