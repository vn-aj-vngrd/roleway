import { Plus } from "lucide-react";
import { SettingsNav } from "@/components/settings-nav";
const providers = [{ mark: "AN", name: "Anthropic", state: "Connected · sk-ant-••••7HJ" }, { mark: "OA", name: "OpenAI", state: "Not configured" }, { mark: "GE", name: "Google Gemini", state: "Not configured" }, { mark: "OR", name: "OpenRouter", state: "Not configured" }, { mark: "OL", name: "Ollama", state: "Local · http://localhost:11434" }];
export default function AiSettingsPage() { return <div className="page"><header className="page-header"><div><h1>Settings</h1><p className="page-subtitle">Provider-neutral models and private credentials.</p></div></header><div className="settings-layout"><SettingsNav active="AI providers" /><main><section className="form-section"><div className="content-section-head"><div><h2>Providers</h2><p className="page-subtitle">Keys are encrypted at rest and never shown again after saving.</p></div><button className="button secondary"><Plus />Add endpoint</button></div>{providers.map((provider) => <article className="provider-row" key={provider.name}><div className="provider-icon">{provider.mark}</div><div className="provider-copy"><div className="provider-name">{provider.name}</div><div className="provider-state">{provider.state}</div></div><button className="button ghost">Configure</button></article>)}</section><section className="form-section"><h2>Model roles</h2><p>Features select a capability role, not a provider-specific model.</p>{[["Default model", "Claude Sonnet"], ["Fast model", "Gemini 2.0 Flash"], ["Reasoning model", "Claude Sonnet"], ["Agent model", "Claude Sonnet"], ["Embedding model", "nomic-embed-text · Ollama"]].map(([label, value]) => <div className="detail-row" key={label}><dt>{label}</dt><dd>{value}</dd></div>)}</section><section className="form-section"><h2>Before data leaves this server</h2><p>Every external request shows which provider receives which workspace context. Local Ollama requests remain on your configured network.</p><label style={{ display: "flex", gap: 9, alignItems: "center", fontSize: 12 }}><input type="checkbox" defaultChecked /> Always show the context manifest before first use with a provider</label></section></main></div></div>; }
