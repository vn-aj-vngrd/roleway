import { ArrowRight, CalendarClock, CheckCircle2, Circle, Play, Plus } from "lucide-react";

const topics = ["Research company", "Understand architecture", "Node event loop", "PostgreSQL indexes", "React rendering", "System design", "Questions for interviewer"];

export default function PreparationPage() {
  return <div className="page narrow"><header className="page-header"><div className="page-header-copy"><h1>Preparation</h1><p className="page-subtitle">Upcoming interviews and focused practice.</p></div><button className="button primary"><Plus />Add interview</button></header><section className="content-section"><div className="section-label">Next interview</div><div style={{ display: "flex", alignItems: "flex-start", gap: 16, paddingBottom: 20, borderBottom: "1px solid var(--line)" }}><div style={{ flex: 1 }}><div className="muted small">Acme · Senior Software Engineer</div><h2 style={{ marginTop: 3 }}>Technical interview</h2><p className="page-subtitle"><CalendarClock size={13} /> Aug 21 at 10:00 AM · 60 min · in 3 days</p></div><button className="button primary">Continue preparation <ArrowRight /></button></div><div style={{ display: "grid", gridTemplateColumns: "120px 1fr", gap: 26, paddingTop: 22 }}><div><div className="score-large">68%</div><div className="muted small">Readiness</div><div className="progress" style={{ marginTop: 10 }}><span style={{ width: "68%" }} /></div></div><div className="prep-list">{topics.map((topic, index) => <div className="task-item" key={topic}>{index < 2 ? <CheckCircle2 className="step-done" size={16} /> : <Circle className="muted" size={16} />}<span>{topic}</span>{index === 2 ? <span className="tag" style={{ marginLeft: "auto" }}>Recommended next</span> : null}</div>)}</div></div></section><section className="content-section"><div className="content-section-head"><div><h2>Practice</h2><p className="page-subtitle">Questions selected from this role’s requirements and your gaps.</p></div><button className="button secondary"><Play />Start mock interview</button></div><div className="document-list">{["Explain Node’s event loop under load", "Design a collaborative issue-tracking system", "Tell me about a product decision you influenced"].map((question, index) => <div className="list-row" key={question}><span className="mono muted">0{index + 1}</span><div><div className="list-title">{question}</div><div className="list-subtitle">{index === 2 ? "Behavioral · Medium" : "Technical · Medium"}</div></div><span className="tag">Not practiced</span><span /><button className="icon-button" aria-label={`Practice ${question}`}><ArrowRight /></button></div>)}</div></section></div>;
}
