# Workflow audit — 14 September 2026

## Verified findings and fixes

- The hosted Supabase project was inactive and its hostname did not resolve. Signup and notification browser journeys failed before authentication. The existing project was resumed through the Management API and verified ACTIVE_HEALTHY. The forward migration was applied; the subsequent migration dry run reported the remote database up to date.
- Account-wide Agent proposals conflicted with the conversation-Workspace target trigger. A forward migration adds an independently validated destination Workspace while preserving conversation attribution. Approval cards disclose that destination.
- Agent ignored persistence errors. Answer, proposals, steps, and final run state now save in one database transaction. Context read failures and invalid targets fail explicitly; failed runs are linked to the saved user message and their details remain visible. Redacted diagnostics distinguish context, persistence, structured-output, and provider-timeout failures.
- Approvals previously used separate writes for claiming, mutation, activity, and completion. A locked transaction now applies all four supported tools atomically, rechecks owner and destination, rejects archived targets, expires seven-day-old proposals, and completes runs after their last decision. Repeated approval is idempotent; a failed mutation rolls back and remains retryable.
- Compatible-provider URL validation allowed IPv6 loopback, URL credentials, and nonstandard ports. Requests now check DNS results and private addresses, reject credentialed/nonstandard endpoints, and disallow redirects. The shared address check recognizes hexadecimal IPv4-mapped IPv6. Existing DNS-check/fetch separation is retained; this is not a claim of transport-level DNS pinning.

- Password recovery ignored Supabase errors and falsely reported success. It now distinguishes rejected requests, verifies recovery/invitation token hashes, and converts implicit invitation sessions into SSR cookies before showing the password form. One-time recovery, actual password update, expired-link rejection, and implicit invitation handling passed browser tests.
- Account deletion omitted CAPTCHA from password reauthentication. The dialog now presents the existing verification widget, passes its token, preserves the account on verification failure, reports the correct error, and signs out after successful deletion.
- The selected OpenRouter Nemotron free model advertises tool calls rather than JSON-schema response format. A real tiny tool response took 73 seconds, exceeding the old 45-second timeout. OpenRouter now uses validated forced-tool output with bounded output tokens and reasoning disabled; OpenRouter has a 240-second timeout and AI pages allow 300 seconds; other providers retain the original 45-second timeout. The separate live browser test passed with `nvidia/nemotron-3-ultra-550b-a55b:free`: save/test credentials, real generation, one proposed task, zero mutation before approval, exactly one persisted approved task, reload, no browser page errors, and no horizontal overflow at 1440px/390px. The complete live test took 3.4 minutes; free-model latency is material.

## Development and release workflow

See [Development workflow](DEVELOPMENT_WORKFLOW.md) and [Deployment and releases](DEPLOYMENT_AND_RELEASES.md). The implementation follows Relay's existing semantic-release configuration, adapted to Roleway's main branch and pnpm monorepo. CI validates PR titles, types, tests, lint, builds, migrations, and Agent transactions. Browser CI requires a separately configured test environment. Releases activate after merge and successful main CI.

## Verification evidence

- Full local typecheck, 54 unit tests, six release workflow tests, lint, and production build passed.
- All repository migrations applied to isolated PostgreSQL 17 using a minimal Supabase Auth schema fixture.
- Transaction tests exercise all four Agent tools, cross-Workspace attribution, other-owner approval denial, repeated approval, rejection, activity, completion, rollback of an invalid multi-proposal response, and complete account cleanup with Agent history.
- After database restoration, the real browser product journey passed: onboarding, Workspace setup, Job capture, Opportunity, application, tasks, interview, and sign-out. Separate checks passed Workspace switching, global search, JSON export, normal-user admin denial then authorized viewer access, cross-account RLS denial, and mark-all-read persistence. Recovery and invitation checks also passed. The final combined browser run passed 14 tests; the opt-in live-provider test was skipped in that run and is tracked separately.
- Authenticated tests used disposable accounts via the Admin API because production CAPTCHA remains enabled. This does not verify public CAPTCHA/password login, email delivery, or successful account deletion with a human challenge.
- GitHub CI and Vercel preview passed for commit `45e836d`. Vercel project configuration confirms Fluid Compute enabled and `main` as production. The final follow-up head is checked separately.
- Existing Vercel production deployment was Ready and aliased to roleway.vanajvanguardia.tech. This proves deployment wiring, not authenticated product health.

## Remaining live verification

- Verify human CAPTCHA/password login, actual email delivery, and the final deployed account connection. Disposable local-browser checks use the restored hosted database but do not prove production environment credentials.
- The user authorized squash merge after current-head checks and review resolution. Verify main CI, the first automated release, and production deployment after merging.

## PR review follow-up

- Excluded archived Workspace Opportunities from Agent focus and generation targets. A forward database guard rejects archived destinations atomically; paused Workspaces remain supported.
- Persisted fixture cookies even without a browser and added authenticated-user identity plus own-profile positive controls before cross-account RLS denial.
- Captured Next Action text and due date before generation. Approval compares that snapshot under lock, expires changed or legacy proposals, preserves manual edits, and explains regeneration. Database regression coverage and a real browser approval test verify this behavior at 1440px and 390px.
- Final standards review found no additional blockers; the spec review freshness finding is addressed.
